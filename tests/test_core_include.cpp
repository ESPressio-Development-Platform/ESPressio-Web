#include <ESPressio_Web.hpp>

static_assert(ESPressio::Web::Version::Major == 0);
static_assert(ESPressio::Web::Version::Minor == 1);
static_assert(ESPressio::Web::Version::Patch == 0);

int main() { return 0; }
